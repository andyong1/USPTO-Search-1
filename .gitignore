node_modules/
.vercel/
.env
.env.local
.DS_Store
# local, derived data + tooling artifacts (never source)
snq-cumulative/
downloads/
graphify-out/
_*.pdf
_*.txt
_*.mjs
# local scheduled-task secrets (POSTGRES_URL / CRON_SECRET)
grounds-secrets.env
# database export output
db-export/
# machine-local Claude Code permissions (not shared)
.claude/settings.local.json
# scratch: .txt copies of scripts + one-off OCR check
collect-*.txt
ocr-check.txt
# derived petition-decisions data (like snq-cumulative/)
snq-petition-decisions/
# audit/report exports
*.docx
